// Сложение
function sum() {
    let one = document.getElementById(`number1`);
    let two = document.getElementById(`number2`);
    document.writeln(Number(one) + Number(two));
}

// Вычитание
function deduct() {
    let one = document.getElementById(`number1`);
    let two = document.getElementById(`number2`);
    document.writeln(Number(one) - Number(two));
}

// Умножение
function multiply() {
    let one = document.getElementById(`number1`);
    let two = document.getElementById(`number2`);
    document.writeln(Number(one) * Number(two));
}

// Деление
function divide() {
    let one = document.getElementById(`number1`);
    let two = document.getElementById(`number2`);
    document.writeln(Number(one) / Number(two));
}

// Итог
function calculate(){

    document.writeln
}